{% block content %}

<div class="form-items">
  <div>Nome</div>
  <input type="text" class="input" name="name" values="{{chef.name}}" placeholder="{{chef.name}}">
</div>
<div class="form-items">
  <div>Avatar</div>
  <input type="url" class="input" name="avatar_url" values="{{chef.avatar_url}}" placeholder="{{chef.avatar_url}}">
</div>
<div style="visibility: hidden;" id="countRecipes">{{chef.total_recipes}}</div>

{% endblock content %}